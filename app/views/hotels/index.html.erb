<h1>Nos Hotels</h1>

<% @hotels.each do |hotel| %>
  <h2><%= hotel.name %></h2>
  <p><%= hotel.adresse %></p>
  <%= link_to "Voir nos chambre", hotel_rooms_path(hotel) %>
    <%# <%= link_to 'Supprimer', hotel_rooms_path(), method: :delete, data: {confirm: "tu es sure?"} %>

  <%= link_to "Supprimer", hotel_path(hotel), data: {turbo_method: :delete, turbo_confirm: "Etes vous sur?"} %>
<% end %>

<p>
<%= link_to'Ajouter votre Hotel', new_hotel_path %>
</p>
